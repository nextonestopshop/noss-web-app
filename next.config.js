// const generateSitemap = require('./scripts/generate-sitemap.js');

module.exports = {
    trailingSlash: true
    // webpack: (config, {
    //     isServer
    // }) => {
    //     if (isServer) {
    //         generateSitemap();
    //     }
    //     return config;
    // }
}